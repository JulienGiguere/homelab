image:
  repository: lscr.io/linuxserver/prowlarr
  tag: 2.0.2-nightly
  pullPolicy: IfNotPresent

strategy:
  type: Recreate

service:
  main:
    ports:
      http:
        port: 8989

ingress:
  main:
    enabled: false

persistence:
  config:
    enabled: true
    mountPath: /config
    existingClaim: prowlarr-config-nfs-pvc

env:
  TZ: "America/Montreal"
