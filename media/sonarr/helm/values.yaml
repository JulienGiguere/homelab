image:
  repository: ghcr.io/home-operations/sonarr
  tag: 4.0.15.2940
  pullPolicy: IfNotPresent

service:
  main:
    ports:
      http:
        port: 8989

ingress:
  main:
    enabled: false

persistence:
  config:
    enabled: true
    mountPath: /config
    existingClaim: sonarr-config-nfs-pvc

  media:
    enabled: true
    mountPath: /media
    existingClaim: media-pvc

env:
  TZ: "America/Montreal"
