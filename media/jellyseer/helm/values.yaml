replicaCount: 1

image:
  repository: ghcr.io/fallenbagel/jellyseer
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ghcr-creds

strategy:
  type: Recreate

env:
  TZ: America/Montreal
  LOG_LEVEL: info

service:
  type: ClusterIP
  port: 5055

ingress:
  enabled: false

volumes:
 - name: downloads
   persistentVolumeClaim:
     claimName: media-pvc

volumeMounts:
  - name: downloads
    mountPath: /downloads
    readOnly: true